# Real downloaded data is large and separately licensed - never commit it.
data/raw/
outputs/
__pycache__/
*.pyc
.pytest_cache/

# Local service endpoints and API key variable names - never commit.
sources.local.yaml
.env
